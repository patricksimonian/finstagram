<form method="post" action="/posts">
    <%= erb(:'shared/p_error_messages', {locals: {object: @post}}) %>
    <div class="form-group">
        <label for="photo_url">Photo Url</label>
        <input type="text" id="photo_url" name="photo_url">
    </div>
    <div class="form-group">
        <input type="submit" value="Submit!">
    </div>
</form>

<div class="photo-container">
    <img src="" alt="no preview available" id="preview"/>
</div>

<script>
    // vanilla javascript
    // var photoUrl = document.getElementById("photo_url");
    // photoUrl.addEventListener("blur", function(event) {
    //   //grab the input value of the photo url input
    //   var urlString = event.target.value;
    //   //insert the urlString into the img src attribute above
    //   var imgPreview = document.getElementById("preview");
    //   imgPreview.setAttribute("src", urlString);
    // });
    
    // this is jquery
    // $("#photo_url").on("blur", function(event) {
    //     var photoUrl = event.target.value;
    //      $("#preview").attr("src", photoUrl); 
    // });
    
</script>